schema_version: "1.0"

name: "backstage-permissioned-route-migration"
version: "0.1.0"
description: "Migrate PermissionedRoute to Route + RequirePermission for @backstage/plugin-permission-react"
author: "Alex Bit"
license: "MIT"
workflow: "workflow.yaml"


targets:
  languages: ["typescript", "tsx"]

keywords:
  - backstage
  - migration
  - permission
  - react-router

registry:
  access: "public"
  visibility: "public"

capabilities: []
