schema_version: "1.0"

name: "backstage-plugin-frontend-system-migration"
version: "0.1.3"
description: "Migrate Backstage frontend plugins from the legacy core-plugin-api model to the new frontend system (frontend-plugin-api): route refs, plugin shell, APIs, page hooks, blast-radius metrics; optional AI and package.json/lockfile steps in the same workflow node, gated by params (off by default)."
author: "Alex Bit <alex@codemod.com>"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"
repository: "https://github.com/alexbit-codemod/backstage-codemods"

targets:
  languages: ["typescript", "tsx"]

keywords:
  - migration
  - backstage
  - upgrade
  - frontend-plugin-api

registry:
  access: "public"
  visibility: "public"

# JSSG transforms use the default safe module set; workflow `run` steps are executed by the CLI.
capabilities: []
